#include "main.h"

/**
 *print_sign - prints sign of a number
 *Return: 1 when +ve 0 when zero -1 when -ve
 */

int print_sign(int n)
{
	n = 0;
	while (n <= 0)
	{
		for (n < 0)
		{
			_putchar ('-');
			return (-1);
		}
		{
			for (n ==0)
			{
				_putchar('0');
				return (0);
			}
		}
	}
	return (1);
}

