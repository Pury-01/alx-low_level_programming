#include <stdio.h>

/**
*main -Entry
*Return : Always 0(success)
*this program print single-digit numbers 
*/

int main(void)
{
  int number = 0;

while (number < 10)
{
   putchar('0' + number);
   number++;
}
   putchar('\n'0;
		 
return (0);
}
